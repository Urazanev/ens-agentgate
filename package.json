{
  "name": "tool-gate",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal ENS-gated auth service for wallet-native agents (MVP).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "demo": "tsx examples/demo-agent-client.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "viem": "^2.24.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
